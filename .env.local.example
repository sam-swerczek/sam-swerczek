# =============================================
# SUPABASE CONFIGURATION
# =============================================
# Get these values from your Supabase project dashboard:
# 1. Go to https://app.supabase.com
# 2. Select your project
# 3. Click Settings (gear icon) > API
# 4. Copy the values below

# Project URL
# Location: Settings > API > Project URL
# Format: https://xxxxxxxxxxxxx.supabase.co
# This is safe to expose in client-side code (prefixed with NEXT_PUBLIC_)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url

# Anon/Public Key
# Location: Settings > API > Project API keys > anon public
# This key is safe to use in client-side code as it respects Row Level Security (RLS)
# It starts with "eyJ..." and is quite long
# Used for: Client-side queries, auth, public data access
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Service Role Key (SECRET - Server-side only!)
# Location: Settings > API > Project API keys > service_role
# WARNING: This key bypasses Row Level Security - NEVER expose in client code!
# Only use this in server-side code (API routes, server actions, etc.)
# Used for: Admin operations, bypassing RLS, server-side data mutations
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# =============================================
# SITE CONFIGURATION
# =============================================

# Site URL
# Development: http://localhost:3000
# Production: https://samswerczek.com (or your custom domain)
# Used for: Absolute URLs, canonical links, social sharing metadata
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================
# AI CONFIGURATION
# =============================================

# Anthropic API Key (for AI Draft Assistant)
# Get your API key from: https://console.anthropic.com/
# 1. Go to https://console.anthropic.com/
# 2. Sign in or create an account
# 3. Navigate to API Keys section
# 4. Generate a new API key
# Used for: AI-powered blog post draft generation in admin portal
ANTHROPIC_API_KEY=your-anthropic-api-key

# =============================================
# EMAIL CONFIGURATION
# =============================================

# Resend API Key (for contact form emails)
# Get your API key from: https://resend.com/
# 1. Go to https://resend.com/ and sign up
# 2. Navigate to API Keys section
# 3. Create a new API key
# 4. Add your domain and verify it (or use their test domain for development)
# Used for: Sending contact form submissions via email
RESEND_API_KEY=your-resend-api-key

# =============================================
# OPTIONAL CONFIGURATION
# =============================================

# Analytics (if using Vercel Analytics, Plausible, etc.)
# NEXT_PUBLIC_ANALYTICS_ID=

# Feature Flags
# NEXT_PUBLIC_ENABLE_COMMENTS=false
# NEXT_PUBLIC_ENABLE_SEARCH=false
